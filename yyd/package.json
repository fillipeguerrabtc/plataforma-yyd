{
  "name": "yyd-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "prisma:gen": "prisma generate",
    "db:push": "prisma db push"
  },
  "devDependencies": {
    "prisma": "^5.19.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@prisma/client": "^5.19.0"
  }
}
